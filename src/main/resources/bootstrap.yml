spring:
  application:
    name: nacos-study-api
  cloud:
    nacos:
      config:
        # 指定nacos服务端地址
        server-addr: localhost:8848
        # 指定nacos配置中心使用的命名空间
        namespace: 013f54f2-582f-4921-a998-46fdabeb0542
user:
  name: api内bootstrap.yml的name
  message: api内的bootstrap.yml的message